# rs-schema-derive
`rs-schema-derive` is a Rust library that provides procedural macros to derive `rs-schema` types from Rust code structures.

This crate is very much a work in progress. Changes are expected and contributors are welcome!