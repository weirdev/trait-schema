# rs-schema-derive-test
`rs-schema-derive-test` is a Rust library that provides tests for the `rs-schema-derive` procedural macros.

This crate is very much a work in progress. Changes are expected and contributors are welcome!