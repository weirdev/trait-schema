# rs-schema-derive-impl
`rs-schema-derive-impl` is a Rust library that provides the implementation of procedural macros for `rs-schema-derive`.

This crate is very much a work in progress. Changes are expected and contributors are welcome!